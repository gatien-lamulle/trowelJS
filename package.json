{
  "name": "troweljs",
  "version": "1.0.0",
  "description": "A Javascript module to facilitate the construction of a website brick by brick.",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc -p tsconfig.json && npx tsc -p tsconfig-cjs.json"
  },
  "keywords": [
    "brick",
    "construction",
    "easily",
    "block",
    "pieces"
  ],
  "author": "Gatien Lamulle",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.0.3"
  },
  "devDependencies": {
    "@types/node": "^18.15.11"
  }
}
